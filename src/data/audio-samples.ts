export interface AudioSample { id: string; title: string; description: string; src: string; }
/** Add only approved, licensed audio files; do not substitute synthetic testimonials. */
export const audioSamples: AudioSample[] = [];
